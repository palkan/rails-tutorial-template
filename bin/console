#!/usr/bin/env -S node --disable-warning=ExperimentalWarning

import initVM from "../src/rails.js";
import IRBRepl from "../src/irb.js";

const vm = await initVM();
const irb = new IRBRepl(vm);

irb.start();
